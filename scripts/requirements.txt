# OpenAI SDK (gpt-image-2 support)
openai>=1.55.0

# Async HTTP (the OpenAI SDK already uses httpx; aiohttp is here for any
# extra tooling that might want a separate async client)
aiohttp>=3.9.0

# YAML templates and task lists
PyYAML>=6.0.1

# Image processing (slicing, alignment, sprite-sheet packing)
Pillow>=10.4.0

# Transparent-background removal (gpt-image-2 does not support transparent
# output, so we cut it out locally). rembg is large and downloads an ONNX
# model on first run.
rembg>=2.0.59
onnxruntime>=1.18.0

# .env file loader
python-dotenv>=1.0.1

# CLI prettifier (progress bars, tables)
rich>=13.7.0

# Local Agent Hub web dashboard
fastapi>=0.111.0
uvicorn>=0.30.0
jinja2>=3.1.4
python-multipart>=0.0.9
Markdown>=3.6

# BiRefNet 抠图（可选，仅 birefnet_cutout.py 需要）
# transformers>=4.46.0
# torch>=2.5.0
# torchvision>=0.20.0

# 视频抽帧 & 帧漂移检测（可选）
# opencv-python>=4.10.0
# mediapipe>=0.10.0

# Replicate API（可选，BiRefNet 云端后端）
# replicate>=0.30.0
